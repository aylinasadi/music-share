# Music Share
A music sharing web app built with Django and React, powered by Spotify.

## Setup
1. Install Python dependencies: `pip install -r requirements.txt`
2. Install Node dependencies: `cd frontend && npm install`
3. Run migrations: `python manage.py migrate`
4. Start Django: `python manage.py runserver`
5. Start Webpack: `cd frontend && npm run dev`